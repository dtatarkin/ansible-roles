apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: cnpg-user-{{ item.name | replace('_', '-') }}
  namespace: {{ cnpg_namespace }}
stringData:
  username: {{ item.name }}
  password: {{ cnpg_passwords[item.name] }}
